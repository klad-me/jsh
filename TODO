- cp
- mv
- rm recursively
- append, appendBinary
- make exec work like pipe( stdin, [ "ls", "-la" ], [ "grep", ".sh" ], stdout, stderr);
- interactive shell


+ cd
+ pwd
+ stat
+ read stdin
+ set, unset
+ env
+ fork and exec function
	bg
	wait
	kill
+ sleep
+ checkpid
+ code from command line
+ printf, sprintf
+ utf8_assembly as module
+ read use strbuf
+ exec(["ls", "-la" ], stdin, stdout, stderr);
