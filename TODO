- cp
- mv
- rm recursively
- append, appendBinary
- exec
- pipe
- exec(["ls", "-la" ], stdin, stdout, stderr);
- pipe( stdin, [ "ls", "-la" ], [ "grep", ".sh" ], stdout, stderr);
- read use strbuf


+ cd
+ pwd
+ stat
+ read stdin
+ set, unset
+ env
+ fork and exec function
	bg
	wait
	kill
+ sleep
+ checkpid
+ code from command line
+ printf, sprintf
