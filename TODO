- cp
- mv
- rm recursively
- printf, sprintf
- append, appendBinary
- exec
- pipe
- code from command line
- wait pid for specified time
- exec(["ls", "-la" ], stdin, stdout, stderr);
- pipe( stdin, [ "ls", "-la" ], [ "grep", ".sh" ], stdout, stderr);


+ cd
+ pwd
+ stat
+ read stdin
+ set, unset
+ env
+ fork and exec function
	bg
	wait
	kill
+ sleep
